# Telegram бот з міні-додатком для OLX

## Архітектура проекту

### Структура папок

```
бот/
├── config/                    # Конфігурація
│   ├── .env                  # Токени (BOT_TOKEN, MINI_APP_URL)
│   └── requirements.txt      # Залежності Python
├── bot/                      # Основний код бота
│   ├── handlers/             # Обробники команд
│   │   ├── __init__.py      # Модуль обробників
│   │   └── start.py         # Обробник /start з кнопкою WebApp (GitHub Pages)
│   ├── utils/                # Утиліти
│   │   ├── __init__.py      # Модуль утиліт
│   │   └── logger.py        # Система логування (консоль + файл)
│   └── main.py              # Точка входу - запускає бота
├── logs/                     # Логи бота
│   └── bot.log              # Файл логів (з датою та рівнями)
├── mini_app/                 # Фронтенд міні-додатку
│   └── увійти.html          # Початкова сторінка авторизації OLX
└── README.md                 # Архітектура проекту
```

## Опис файлів

### config/.env
- `BOT_TOKEN` - токен Telegram бота
- `MINI_APP_URL` - базовий URL міні-додатку на GitHub Pages

### config/requirements.txt
- `aiogram==3.1.1` - бібліотека для Telegram бота
- `python-dotenv==1.0.0` - читання .env файлів

### bot/main.py
- Завантажує токен з config/.env
- Створює Telegram бота (aiogram)
- Створює диспетчер для обробки повідомлень
- Підключає обробники команд
- Запускає polling бота
- Логує запуск, зупинку та помилки

### bot/handlers/start.py
- Обробник команди `/start`
- Відповідає привітальним повідомленням
- Створює кнопку "📱 Відкрити додаток" з WebApp
- Читає URL міні-додатку з `MINI_APP_URL` в `.env` (GitHub Pages)
- Логує обробку команди
- Обробка помилок при відправці повідомлень

### bot/utils/logger.py
- Система логування для бота
- Вивід в консоль: просто текст без префіксів
- Вивід в файл: з датою та рівнем (INFO, ERROR, WARNING)
- Файл логів: `logs/bot.log`
- Функція `log(level, message)` для логування

### mini_app/увійти.html
- Початкова сторінка міні-додатку
- Сторінка авторизації OLX
- Хоститься на GitHub Pages

## Система роботи

### Запуск
1. Завантажується токен з `config/.env`
2. Створюється Telegram бот
3. Запускається polling бота

### Робота з міні-додатком
1. Користувач пише `/start` боту
2. Бот відповідає з кнопкою "📱 Відкрити додаток"
3. Користувач натискає кнопку
4. Telegram відкриває WebApp з GitHub Pages URL
5. Користувач бачить сторінку авторизації OLX

## Поточний стан

### Реалізовано
- ✅ Базова структура проекту
- ✅ Конфігурація (.env з BOT_TOKEN та MINI_APP_URL, requirements.txt)
- ✅ Telegram бот з обробником /start
- ✅ Кнопка WebApp з GitHub Pages URL (HTTPS)
- ✅ URL міні-додатку налаштовується через .env
- ✅ Початкова сторінка міні-додатку (увійти.html) завантажена на GitHub Pages
- ✅ Система логування (консоль без префіксів, файл з датою та рівнями)
- ✅ Обробка помилок у handler'ах
- ✅ .gitignore для захисту секретів

### Планується
- ⏳ База даних (SQLite)
- ⏳ Авторизація OLX через міні-додаток
- ⏳ API endpoints для роботи з акаунтами
- ⏳ API endpoints для роботи з чатами
- ⏳ Моніторинг повідомлень
- ⏳ Система оновлення токенів
